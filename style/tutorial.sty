% 講習会名 (例: "CMSI柏ハンズオン")
\author{第62回お試しアカウント付き講習会「ライブラリ利用:科学技術計算の効率化入門}

% 講習会日時(4桁-2桁-2桁. 例: "2013-04-03")
\date{2016-09-07}

% \institute{ALPS Collaboration \\ http://alps.comp-phys.org/}

% しおりの文字化けを防ぐ
\AtBeginDvi{\special{pdf:tounicode EUC-UCS2}}

% navigation が正しく動くようにする
% https://gist.github.com/zr-tex8r/4980892
\usepackage{bxdpx-beamer}

% 和文メトリックの調整（代わりに otf を読み込んでも良い）
\usepackage{minijs}

% 和文既定をゴシックに変更
\renewcommand{\kanjifamilydefault}{\gtdefault}

\mode<presentation>
{
  \usetheme{Antibes}
  \usecolortheme{rose}
  \setbeamercovered{transparent}
}

\usepackage[english]{babel}

% ディレクトリ構造の描画に使用
\usepackage{dirtree}

% CMSI ロゴ
%\pgfdeclareimage[height=0.5cm]{university-logo}{cmsi}
%\logo{\pgfuseimage{university-logo}\hspace*{2em}}

% ソースコード表示用
\usepackage{listings,jlisting}
\input{python_listing.tex}

% 実行コマンド記述用のスタイル(bashシェル用で、生成されたPDFからコピー＆ペーストできるように空白を保つ)
\lstdefinestyle{shstyle}{
    language=bash,
    basicstyle=\ttfamily\tiny,
    frame=single,
    breaklines=true,                % sets automatic line breaking
    breakautoindent=false,
    breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
    backgroundcolor=\color[cmyk]{0., 0. ,0,, 0.05},
    rulecolor=\color[cmyk]{0., 0. ,0,, 0.5},
    deletekeywords={local, test}  % bashの予約語としてハイライトされないようにする語を設定
}

\lstset{frame=single}
\lstset{framesep=2pt}
\lstset{rulecolor=\color{codeframe}}
\lstset{upquote=true}
\lstset{basicstyle=\ttfamily}
\lstset{showstringspaces=false}
\lstset{showspaces=false}
\lstset{showtabs=false}                 % show tabs within strings adding
\lstset{literate={-}{-}1}
\lstset{columns=flexible}
\lstset{keepspaces=true}
\makeatletter
\def\lst@outputspace{{\ifx\lst@bkgcolor\empty\color{white}\else\lst@bkgcolor\fi{ }}}
\makeatother

% 箇条書きの２階層目のマーカを三角にする
\setbeamertemplate{itemize subitem}[triangle]

% ページ番号の挿入
\setbeamertemplate{footline}[frame number]

% スライド下部にある不要な画像を消す
\setbeamertemplate{navigation symbols}{}

% hyperref setting
\definecolor{links}{HTML}{2A1B81}
\hypersetup{colorlinks,linkcolor=,urlcolor=links}

% アンダースコアが入っている文字列をバックスラッシュなしで与えられるようにする
\usepackage[strings]{underscore}
% ファイル名を与えたときに、githubへのリンクを作るコマンド
\newcommand{\RokkoFilename}[1]{%
\href{https://github.com/t-sakashita/rokko/tree/master/#1}{#1}}

